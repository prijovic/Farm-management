export class ParcelOperation {
    constructor(
        public id: string,
        public creationDate: string,
        public lastEditDate: string,
        public name: string,
        public description: string,
        public status: number
    ) {
    }
}
